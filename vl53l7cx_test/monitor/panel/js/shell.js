// Sidebar mode switching (C02) and mode routing / cross-mode state (C03).
//
// C01 only defines the shell's static DOM in index.html (sidebar nav items,
// five .mode-section containers, "monitor" active by default) and this
// stub file; the interactive behavior — click/keyboard switching, hiding
// the previous section, persisting state across a switch — belongs to
// C02 and C03.
