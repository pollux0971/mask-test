# D10 — Crosstalk 分析報告

> ⚠️ **本報告使用合成資料，數字不是真實結論。**真實結論待 `A16`（韌體 `$A`/`AMB` 支援）與 `E02`（Crosstalk 資料蒐集）完成後，用真實錄音重跑本模組。

## PASS/FAIL 判定
- 感測器 A：**PASS**（最差 zone 3，Δ=1.249 mm，門檻 2.0 mm）
- 感測器 B：**PASS**（最差 zone 3，Δ=1.206 mm，門檻 2.0 mm）

## Ambient 變化率（越靈敏、越早顯現的干擾指標）
- 感測器 A：最大變化率在 zone 9，+33.3%
  ⚠️ 距離 Δ 通過門檻，但 ambient 變化率達 +33.3%，屬於「目前還沒事，但邊際很小」的訊號，值得留意。
- 感測器 B：最大變化率在 zone 9，+32.5%
  ⚠️ 距離 Δ 通過門檻，但 ambient 變化率達 +32.5%，屬於「目前還沒事，但邊際很小」的訊號，值得留意。

## Fallback 建議
兩顆感測器皆 PASS，不需要 fallback。
