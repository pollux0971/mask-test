CONTRACTS.md 有變更，你手上的 story 可能受影響。

來源：B16 完成回報提出的兩點建議，經複核後**兩點都成立，且範圍比原提案更廣**。

## 變更 1／3 — §3.2.1 σ 下限由 1e-3 改為 1/√12

原本：np.maximum(sigma, 1e-3)
現在：np.maximum(sigma, SIGMA_FLOOR)，SIGMA_FLOOR = 1/√12 ≈ 0.28868
      單位 = 該通道的傳輸單位（距離為 mm，signal 為 100 kcps/spad）

原因：$T 的所有通道都是整數。一個量化到 1 個單位的量測，其雜訊 σ 不可能
      有意義地小於量化本身——均勻量化誤差的 σ 是 Δ/√12。

實測後果：一個貼著剛性表面的 zone（真實 17.0 mm、類比雜訊 0.15 mm）量化後
      baseline_sigma ≈ 0.026 mm。讀到 18 mm 那一幀：
        1e-3  守衛 → z = 39     （其他 zone 的 z 都在 ±3）
        1/√12 守衛 → z = 3.47   （合理）
      前者會讓那一個 zone 單獨主宰整個特徵向量，且無任何錯誤訊息。

⚠ 這不是只有 mock 會遇到。真機上任何一個貼著硬表面、或視野內沒有目標而
  回傳固定值的 zone 都會踩到。

## 變更 2／3 — §3.2.2 D11 的 SNR 分母原本完全沒有守衛

D11 story 原文：snr_zone = |Δ_round - Δ_spread| / sigma_baseline
                                                 ^^^^^^^^^^^^^^ 裸的，沒有 max()

這是整條分析鏈裡最嚴重的一個洞，因為 D11 的活躍 zone 索引直接餵給 D01：

  剛性表面 zone → σ≈0 → SNR = inf → 活躍 zone 排名第一
               → 主宰特徵向量 → 汙染 DTW 距離
               → D05 / D06 / D07 的辨識結果全部失真

全程沒有任何錯誤訊息。加守衛後同一個 zone 的 SNR 是 3.26，排名回到正常位置。

## 變更 3／3 — §2.2 VAD 四個時間欄位「偵測不到」的表示

新增規定：
  vad_start_us / vad_end_us / lip_onset_us / voice_onset_us

  Python API（to_trial_attrs()）  → None
  HDF5 attrs（B07 寫入）          → 整個 attr 不寫入

不可填 0、-1、或 capture 視窗邊界。填視窗邊界最糟：「完全沒偵測到」會長得像
「整段都在動」，下游算統計不會報錯，只會算出錯的答案。

屬性缺席時 grp.attrs["lip_onset_us"] 會拋 KeyError——大聲失敗，而不是安靜地
給出錯誤答案。消費端用 .attrs.get()。

注意：silent 模式下 voice_onset_us 必然缺席，但 lip_onset_us 仍應存在。
      不可假設四個欄位同時存在或同時缺席。

## 你要做的

1. 重讀 CONTRACTS.md 的 §2.2、§3.2.1、§3.2.2、§3.2.3
2. 檢查你已經寫的程式碼：
   - 有沒有任何地方用 1e-3 當 σ 下限？
   - 有沒有任何除以 sigma / std 而沒有加守衛的地方？
   - 有沒有把「偵測不到」填成 0、-1 或視窗邊界？
   - 讀 HDF5 attrs 時有沒有假設四個 VAD 欄位一定存在？
3. 回報下列其中之一：
   - 「不受影響」+ 一句話說明為什麼
   - 「需要修改」+ 具體哪些檔案哪些行 + 你打算怎麼改（先別動手）
   - 「這個變更會讓 <某條驗收條件> 無法達成」+ 說明

特別提醒 D 軌：如果你已經用假資料跑過任何 SNR 或 z-score，那些數字要重跑。
不要自己判斷「我的假資料沒有 σ≈0 的 zone 所以沒差」——真實資料會有。
